=TEXTJOIN(CHAR(10), TRUE, INDIRECT("写入!A" & ROW() & ":S" & ROW()), INDIRECT("写入!T" & ROW()-1 & ":Y" & ROW()-1))

=TEXTJOIN(CHAR(10), TRUE, INDIRECT("写入!T" & ROW()-1 & ":Y" & ROW()-1), INDIRECT("写入!A" & ROW() & ":S" & ROW()))

# 更改内容如下

=TEXTJOIN(CHAR(10), TRUE, INDIRECT("写入!A" & ROW() & ":Y" & ROW()))

=TEXTJOIN(
    CHAR(10), 
    TRUE, 
    INDIRECT("写入!A" & ROW()),
    INDIRECT("写入!T" & ROW()-1 & ":Y" & ROW()-1), 
    INDIRECT("写入!B" & ROW() & ":S" & ROW())
)
